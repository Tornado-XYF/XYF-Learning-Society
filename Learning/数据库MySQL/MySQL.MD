# 数据库MySQL

## 启动与关闭

以管理员身份运行命令提示符CMD
输入以下命令启动MySQL服务：

```.
net start mysql
```

输入以下命令关闭MySQL服务：

```.
net stop mysql
```

## DBMS

DBMS是数据库管理系统的缩写，它是一种用于管理和操作数据库的软件系统。而数据库的数据是存储在一种表结构当中

## 关系型数据库（RDBMS）

建立在关系模型基础上的数据库管理系统，它使用表格来组织和存储数据。每个表格都有一个唯一的名称，并且包含多个列（字段）和行（记录）。